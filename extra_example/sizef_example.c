// find c programme data type memory bytes int, float, double, char
#include<stdio.h>
int main()
{
    int a ;
    float b;
    double c ;
    char d;
    // find data type memory bytes so use sizef(variable name);
    printf("Size of int = %d bytes\n",sizeof(a));
    printf("Size of float = %d bytes\n",sizeof(b));
    printf("Size of double = %d bytes\n",sizeof(c));
    printf("Size of char = %d bytes\n",sizeof(d));
    
}